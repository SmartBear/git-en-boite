#!/usr/bin/env node
const { execSync } = require('child_process')
const exec = cmd => execSync(cmd, { stdio: 'inherit' })

if (process.env.TERM)
  exec('clear')

exec('npx mocha')

exec('./bin/cucumber')
