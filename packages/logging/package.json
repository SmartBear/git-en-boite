{
  "name": "git-en-boite-logging",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "description": "Adapters for logging on the server",
  "scripts": {
    "build:clean": "rm -rf dist *.tsbuildinfo",
    "build": "tsc --build",
    "lint:fix": "eslint src/**/*.ts --quiet --fix",
    "lint": "eslint src/**/*.ts",
    "test:wip": "mocha -g @wip",
    "test": "mocha -g @wip -i"
  },
  "devDependencies": {
    "@types/koa": "2.13.1",
    "@types/mocha": "9.1.0",
    "hamjest": "3.7.3",
    "koa": "2.13.1",
    "mocha": "9.2.0",
    "supertest": "6.2.2"
  },
  "dependencies": {
    "@types/node": "15.6.1",
    "@types/pino": "6.3.12",
    "git-en-boite-core": "0.0.0",
    "pino": "6.14.0",
    "pino-pretty": "5.1.3"
  }
}
